import app from "../server/app.js";

// Vercel turns every file in this directory into a serverless function, so api/index.js is
// the only file that belongs here. The rest of the backend lives in /server.
export default app;
